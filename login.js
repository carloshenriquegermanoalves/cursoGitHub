//Trabalhando no sistema de login

function login(name, password) {
    if (name == name) {
        if (password == password) {
            return true;
        }
    }
    return false;
}

//Encerrando o sistema de login